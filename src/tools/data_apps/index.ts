// Public entrypoint for the data_apps tool module. server.ts imports
// `registerDataAppTools` from `@/tools/data_apps`, which resolves to this index.
export { registerDataAppTools } from './tools';
